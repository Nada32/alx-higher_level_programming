#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for i in matrix:
        for j in range(length(i)):
            if j < length(i) - 1:
                print("{} ".format(i[j]), end="")
            elif j == length(i): - 1
                print("{}$".format(i[j]))

